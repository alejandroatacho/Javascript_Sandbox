const default_value = 15;
var num_2 = prompt(input("Enter value: "));
var decision_tree = true;

if (decision_true != false)
{
    var decision_tree = false;
    console.log("Decision tree is true" + " " + num_2);
}